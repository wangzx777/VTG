# VTG 工作区日志 — 2026-W34

## 2026-08-17 20:24:33 CST

- 任务：建立按周记录的工作区变更日志。
- 文件：`AGENTS.md`；`Log/2026-W34.md`
- 变更：加入工作区级日志规则，并创建第一份 ISO 周日志。此后，修改工作区文件的任务必须追加简要记录；纯读取任务不记录。

## 2026-08-17 20:28:24 CST

- 任务：将工作区日志统一改为中文。
- 文件：`AGENTS.md`；`Log/2026-W34.md`
- 变更：规定日志默认使用中文，并将已有日志内容翻译为中文；文件路径、命令和技术标识保持原样。

## 2026-08-17 20:32:38 CST

- 任务：初始化 Git 版本记录并将 VTG 工作区提交到 GitHub。
- 文件：`.gitignore`；工作区内 256 个可跟踪文件；Git 仓库配置。
- 变更：忽略 `.DS_Store`，将本地分支改为 `main`，配置 `origin` 为 `git@github.com:wangzx777/VTG.git`，并创建初始提交 `81ed748`。首次推送因 GitHub Deploy Key 仅有读取权限而被拒绝，远程仓库尚未写入；启用 `Allow write access` 后可继续推送。

## 2026-08-17 20:55:58 CST

- 任务：使用个人 GitHub SSH 身份完成 VTG 工作区首次推送。
- 文件：Git 仓库 `main` 分支及 `origin/main`；`Log/2026-W34.md`
- 变更：成功向 `git@github.com:wangzx777/VTG.git` 上传 256 个工作区文件并建立远程 `main` 分支；核验本地与远程提交均为 `f1df3ea`。首次上传数据量约 138.11MB，`.venv`、`.DS_Store`、`work/` 和 `tmp/` 未上传。

## 2026-08-17 22:17:57 CST

- 任务：使用 `paper-read` 阅读 Sim-DETR 论文并生成中文翻译与阅读笔记。
- 文件：`literature/sources/ICCV 2025/translations/2025-sim-detr-unlock-detr-for-temporal-sentence-grounding.md`；`literature/sources/ICCV 2025/notes/2025-sim-detr-unlock-detr-for-temporal-sentence-grounding.md`
- 变更：直接读取原 PDF，新增摘要至结论的段落对齐中文翻译和模板化 AI 阅读笔记；未创建持久提取缓存，未修改 `papers.xlsx`，两份输出均通过 `validate_outputs.py` 验证。

## 2026-08-17 22:36:12 CST

- 任务：清理旧版论文阅读产物，并建立 Mac、GitHub 与 4090 主机的同步约定。
- 文件：`AGENTS.md`；`README.md`；`.gitignore`；`.agents/tools/paper-workspace/README.md`；`.agents/tools/paper-workspace/update_read_status.mjs`；`.agents/skills/paper-triage/SKILL.md`；`.agents/skills/paper-triage/scripts/test_xlsx_pipeline.mjs`；`literature/notes/2026-timelens-rethinking-video-temporal-grounding-with-multimodal-llms.md`；`project/.gitkeep`；`repos/.gitkeep`
- 变更：保留 `literature/sources/` 与 `literature/extracted/`，删除已被 venue 内新版笔记替代的旧 TimeLens 笔记、旧读状态更新工具以及空的 `literature/code-maps/`、`literature/topics/` 目录；删除内容仍可从 Git 历史恢复。补充完整仓库 checkout、双机 pull/commit/push、submodule 和训练产物忽略规则，并确保空的 `project/`、`repos/` 可被 Git 建立。

## 2026-08-17 22:44:02 CST

- 任务：在 4090 主机首次部署 VTG 完整工作区。
- 文件：远程目录 `/home/jia/usr_wangzx/VTG`；远程仓库本地 Git 配置
- 变更：确认专用 GitHub Deploy Key 具有仓库访问权限；因服务器直连 GitHub 首次下载过慢，改用只包含 `main` 的临时 Git bundle 经 SSH 完成首次 clone，随后恢复 `origin` 为 `git@github.com:wangzx777/VTG.git` 并配置专用 SSH identity。远程工作树干净，论文、OCR 缓存、skills、`project/` 与 `repos/` 均已就位；临时 bundle 已从两端删除。

## 2026-08-19 19:14:29 CST

- 任务：在 4090 主机尝试跑通 Sim-DETR，并将远程操作日志改为只在 Mac 记录。
- 文件：`AGENTS.md`；远程 `project/reproductions/sim-detr/runs/debug_results/`
- 变更：使用远程项目 `.venv` 和 RTX 4090 完成 QVHighlights 真实特征的 4-batch debug 训练、checkpoint 保存及 1550-sample 完整验证集推理；训练产物由远程 `.gitignore` 排除，未修改上游源码。补充规则：Codex 操作 4090 时只向 Mac 工作区的周日志追加记录，不为记日志直接修改远程 checkout；先前误加到远程的日志条目已撤回。

## 2026-08-19 20:13:41 CST

- 任务：在 4090 主机克隆 FlashVTG，并使用现有 QVHighlights 特征尝试跑通代码。
- 文件：远程 `.gitmodules`；远程 `repos/FlashVTG/`；远程 `repos/FlashVTG/.venv/`；远程 `project/reproductions/flashvtg/runs/smoke/`
- 变更：将官方 FlashVTG 仓库固定为子模块提交 `25b9591`，在项目独立 uv 环境中安装 PyTorch 2.2.2、nncore 0.4.2 及缺失的 scikit-learn 依赖；未下载新数据，直接复用 `/media/jia/MyProject/flashvtg/QVHighlights/features/` 下的 InternVideo2 视频特征和 LLaMA 文本特征。RTX 4090 冒烟测试完成 7 个训练样本的一次前向/反向、1 个验证样本的推理、NMS、指标计算和可读取 checkpoint 写出；未修改官方源码，完整 150 epoch 训练未启动。

## 2026-08-19 21:17:43 CST

- 任务：使用 VTG 根环境和现有 QVHighlights 特征尝试跑通 CausalVTG。
- 文件：远程 `.gitmodules`；远程 `repos/CausalVTG/`；`project/reproductions/causalvtg/qv_internvideo_smoke.py`；远程 `project/reproductions/causalvtg/runs/gpu-smoke-2/`；远程 VTG 根 `.venv/`
- 变更：将官方 CausalVTG 仓库固定为子模块提交 `1e36a44`，仅向现有 VTG 根 uv 环境补充 `nncore==0.4.4` 及其依赖，保留 PyTorch 2.8.0/CUDA 12.8；未下载新数据集。核验 CausalVTG 的 QVHighlights train/val/test 标注对现有 InternVideo2 视频和 LLaMA 文本特征均无缺失；从现有特征生成 16 簇测试字典，并用 4 条训练、4 条验证样本完成 RTX 4090 前向、反向、checkpoint 和 MR/HL 评估。通过符号链接适配上游对文本特征目录名必须包含 `internvideo2` 的硬编码，未修改官方源码；完整 50 epoch 与正式 256 簇结果未运行。

## 2026-08-19 22:11:13 CST

- 任务：在 4090 主机部署 HieraMamba，并使用 TACoS 预处理特征与 VTG 根 uv 环境准备复现。
- 文件：远程 `.gitmodules`；远程 `repos/HieraMamba/`；远程 `datasets/tacos/`、`datasets/glove/`、`datasets/hieramamba-wheels/`；远程 VTG 根 `.venv/`
- 变更：将官方 HieraMamba 仓库固定为子模块提交 `62671f9`；在现有 PyTorch 2.8.0/CUDA 12.8 环境中安装精确匹配的 `mamba-ssm 2.3.1` 与 `causal-conv1d 1.6.2.post1` 预编译 wheel，编译并验证 NMS 扩展。RTX 4090 上 Mamba2 前向成功，Hydra 可导入，HieraMamba 完整模型可构造；Stanford GloVe 300d 已下载并通过 ZIP 完整性检查。TACoS 官方包已在 Mac 端通过作者 MD5 校验，因两机 Tailscale 使用 DERP 中继，剩余传输已交给本地 `screen` 会话 `hieramamba_tacos` 后台续传；数据尚未重组解压，真实数据训练 smoke test 尚未执行。

## 2026-08-19 22:31:45 CST

- 任务：在 4090 主机依次启动 Sim-DETR 与 FlashVTG 的 QVHighlights 正式训练。
- 文件：`project/reproductions/overnight-qvhighlights/train_sequential.sh`；远程 `project/reproductions/overnight-2026-08-19/preflight/`；远程 `project/reproductions/overnight-qvhighlights/runs/20260819-qvhighlights-official/`
- 变更：核验两套训练/验证标注对应的视频与文本特征均无缺失，并分别完成真实数据的 GPU 前向、反向、验证和 checkpoint 短测试。新增可复用的串行启动脚本，使用 VTG 根 `.venv` 训练 Sim-DETR 200 epoch，随后使用已验证的 `repos/FlashVTG/.venv` 训练 FlashVTG 150 epoch；根环境不安装与 PyTorch 2.8 不兼容的旧版 `torchtext`。后台任务通过 `nohup + setsid` 启动，Sim-DETR 已进入第 1 个 epoch；日志、checkpoint、指标和退出状态均写入上述远程运行目录。

## 2026-08-20 10:08:43 CST

- 任务：使用 `paper-read` 阅读 FlashVTG 论文并生成中文翻译与阅读笔记。
- 文件：`literature/sources/WACV 2025/translations/2025-flashvtg-feature-layering-and-adaptive-score-handling-network-for-video-temporal-grounding.md`；`literature/sources/WACV 2025/notes/2025-flashvtg-feature-layering-and-adaptive-score-handling-network-for-video-temporal-grounding.md`
- 变更：直接读取并核对原 PDF，新增摘要至致谢的段落对齐中文翻译和模板化 AI 阅读笔记；未创建持久提取缓存，未修改 `papers.xlsx`，两份输出均通过 `validate_outputs.py` 验证。
